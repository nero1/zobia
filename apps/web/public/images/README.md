# images

Site logo / brand images go here.

- `logosmall.png` — small square/horizontal wordmark used in the Home tab bar's
  logo tab (`components/home/HomeTabs.tsx`). Not present yet — the product
  owner will upload it. Until then that component renders a text/emoji "Z"
  placeholder, so the page keeps working with or without this file.
