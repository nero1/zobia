<?xml version="1.0" encoding="utf-8"?>
<!--
  Legacy (pre-Android-12, API < 31) backup scoping — ZB-AND-04 fix.
  Same exclusion as data_extraction_rules.xml, using the older API 23+
  full-backup-content schema, since `allowBackup="true"` still applies
  Auto Backup to Google Drive on these OS versions too.
-->
<full-backup-content>
    <exclude domain="sharedpref" path="CapacitorStorage.xml" />
    <exclude domain="sharedpref" path="zobia_secure_tokens.xml" />
</full-backup-content>
