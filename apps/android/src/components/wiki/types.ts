/** Shared small types for the wiki components (kept out of WikiPageEditor.tsx so route files can import the type without pulling the component). */
export type ContentFormat = 'markdown' | 'plaintext';
